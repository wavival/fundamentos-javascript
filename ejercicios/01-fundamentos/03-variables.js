/*
====================================
TEMA
====================================

Variables

====================================
OBJETIVO
====================================

Usar `let`, `const` y `var` correctamente.

====================================
RETO 1
====================================

Declara un perfil de una persona con estos datos:
- `nombre` como `const`
- `edad` como `let`
- `pais` como `const`
- `ciudadActual` como `var`

Luego crea una frase en una sola variable que diga exactamente:
`Hola, soy <nombre>, tengo <edad> años y vivo en <ciudadActual>, <pais>.`

Resultado esperado:
- Las 4 variables quedan declaradas con el tipo indicado.
- La frase final usa los valores reales de las variables.
- La edad puede cambiar una vez antes de construir la frase.
- No basta con mostrar variables sueltas; debe existir una frase completa.

====================================
RETO 2
====================================

Simula la edición de un dato en un formulario.

1. Declara una variable `let` llamada `titulo`.
2. Asígnale un valor inicial.
3. Cambia ese valor dos veces más como si el usuario estuviera corrigiendo el texto.
4. Guarda el valor final en una nueva variable `const` llamada `tituloConfirmado`.

Resultado esperado:
- `titulo` cambia al menos 3 veces en total.
- `tituloConfirmado` conserva el último valor de `titulo`.
- Queda claro cuál era el texto inicial y cuál terminó siendo el definitivo.

====================================
RETO 3
====================================

Construye el precio final de una compra con una constante que no cambie.

1. Declara una `const` llamada `iva`.
2. Declara una `let` llamada `subtotal`.
3. Calcula el total sumando el IVA al subtotal.
4. Guarda el total en una variable llamada `totalConIVA`.

Resultado esperado:
- `iva` permanece igual durante todo el ejercicio.
- El cálculo usa la constante y no un número escrito directamente en el total.
- El valor final representa una compra real, no una suma arbitraria.

====================================
RETO 4
====================================

Escribe una función llamada `prepararPedido` que arme un resumen de pedido usando variables intermedias.

La función debe:
- recibir el nombre del producto y su precio base,
- declarar una variable intermedia para el descuento,
- declarar otra para el precio final,
- devolver un texto con este formato:
`Pedido listo: <producto> cuesta <precioFinal>`

Resultado esperado:
- La función no devuelve el cálculo directo sin usar variables intermedias.
- El precio final cambia según el descuento aplicado.
- El texto final deja claro el producto y el valor calculado.

====================================
RETO 5
====================================

Simula una compra completa en una tienda.

Usa al menos 4 variables para representar:
- cantidad de productos,
- precio unitario,
- costo de envío,
- descuento.

Después calcula el total final.

Resultado esperado:
- Cada dato importante vive en su propia variable.
- El total final combina multiplicación, suma y resta.
- Se entiende cómo se llegó al valor final sin leer operaciones aisladas.
- El ejercicio representa una situación cotidiana, no solo una fórmula matemática.

====================================
CHECKLIST
====================================

[ ] Entiendo el concepto
[ ] Puedo resolver los ejercicios sin ayuda
[ ] Puedo explicar la solución
[ ] Puedo escribir el código desde cero
*/


// ====================================
// SOLUCIONES DEL ESTUDIANTE
// ====================================
